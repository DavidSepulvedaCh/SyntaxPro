---

---

<div class="welcome-modal" id="welcomeModal">
  <div class="modal-overlay" id="modalOverlay"></div>
  <div class="modal-content">
    <button class="close-btn" id="closeModal">×</button>

    <div class="modal-body">
      <h2>Querido/a participante,</h2>

      <p>
        Me encanta enormemente darles la bienvenida al <strong
          >Seminario SyntaxPro</strong
        >. Como fonoaudióloga he diseñado este programa para transformar su
        práctica en intervención morfosintáctica.
      </p>

      <p class="section-title">Durante estas 4 semanas, accederán a:</p>

      <ul class="benefits-list">
        <li>• 8 sesiones en vivo conmigo</li>
        <li>• Investigaciones de efectividad con resultados cuantificables</li>
        <li>• Webinars con expertos</li>
        <li>• Comunidad de profesionales</li>
      </ul>

      <p class="closing">Estoy aquí para acompañarlos en cada paso.</p>

      <div class="signature">
        <p>Con entusiasmo,</p>
        <p class="name">Maritza Niño</p>
        <p class="title">Fonoaudióloga</p>
      </div>
    </div>
  </div>
</div>

<style>
  .welcome-modal {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem;
  }

  .welcome-modal.hidden {
    display: none;
  }

  .modal-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
  }

  .modal-content {
    position: relative;
    background: white;
    border-radius: 16px;
    max-width: 900px;
    width: 100%;
    max-height: 85vh;
    overflow-y: auto;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
    z-index: 1001;
  }

  .close-btn {
    position: absolute;
    top: 1rem;
    right: 1rem;
    background: #f1f5f9;
    border: none;
    width: 36px;
    height: 36px;
    border-radius: 8px;
    font-size: 24px;
    cursor: pointer;
    color: #64748b;
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 1;
    z-index: 1002;
  }

  .modal-body {
    padding: 2.5rem;
  }

  .modal-body h2 {
    font-size: 1.75rem;
    color: var(--color-dark);
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 2px solid #e2e8f0;
  }

  .modal-body p {
    color: var(--color-text);
    line-height: 1.7;
    margin-bottom: 1.25rem;
    font-size: 1.05rem;
  }

  .modal-body strong {
    color: var(--color-primary);
    font-weight: 600;
  }

  .section-title {
    font-weight: 600;
    color: var(--color-dark);
    margin-top: 1.5rem;
  }

  .benefits-list {
    list-style: none;
    padding: 0;
    margin: 1rem 0 1.5rem;
  }

  .benefits-list li {
    padding: 0.75rem 0;
    color: var(--color-text);
    font-size: 1.05rem;
    line-height: 1.6;
  }

  .closing {
    font-style: italic;
    color: var(--color-primary);
    font-weight: 500;
    margin-top: 1.5rem;
  }

  .signature {
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 1px solid #e2e8f0;
  }

  .signature p {
    margin-bottom: 0.5rem;
  }

  .signature .name {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-dark);
  }

  .signature .title {
    color: var(--color-text-light);
    font-size: 1rem;
  }

  @media (max-width: 768px) {
    .modal-body {
      padding: 2rem 1.5rem;
    }

    .modal-body h2 {
      font-size: 1.5rem;
    }

    .modal-body p {
      font-size: 1rem;
    }
  }
</style>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const welcomeModal = document.getElementById("welcomeModal");
    const closeModal = document.getElementById("closeModal");
    const modalOverlay = document.getElementById("modalOverlay");

    // Verificar si el usuario ya vio el mensaje
    const hasSeenWelcome = localStorage.getItem("hasSeenWelcome");

    if (hasSeenWelcome === "true") {
      welcomeModal?.classList.add("hidden");
    }

    function closeWelcomeModal() {
      welcomeModal?.classList.add("hidden");
      localStorage.setItem("hasSeenWelcome", "true");
    }

    // Cerrar el modal al hacer clic en el botón de cerrar
    closeModal?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      closeWelcomeModal();
    });

    // Cerrar el modal al hacer clic en el overlay
    modalOverlay?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      closeWelcomeModal();
    });
  });
</script>
